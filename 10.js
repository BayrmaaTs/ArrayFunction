/*Write a JavaScript program to execute a provided function once for each array element,
starting with the array's last element.*/
const element=['a','b','c'];
console.log(element.reverse());
element.forEach( (k)=> console.log(k) );

